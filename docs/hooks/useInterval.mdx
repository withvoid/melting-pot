---
name: useInterval
menu: Hooks
---

# useInterval

```jsx
const [count, setCount] = useState(0);
useInterval(() => {
  setCount(count + 1);
}, 1000);
return <p>{count}</p>;
```

## Examples

coming soon

## API

coming soon
