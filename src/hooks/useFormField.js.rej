diff a/src/hooks/useFormField.js b/src/hooks/useFormField.js	(rejected hunks)
@@ -8,8 +8,5 @@ const useFormField = (initialValue = '') => {
-  React.useEffect(
-    () => {
-      if (value.length > 0) {
-        setDirty(true);
-      }
-    },
-    [value],
-  );
+  React.useEffect(() => {
+    if (value.length > 0) {
+      setDirty(true);
+    }
+  }, [value]);
